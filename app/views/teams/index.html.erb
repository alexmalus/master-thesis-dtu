<% if current_user.has_role? :member, User %>	

	<div class="container-fluid">
	  <div class="row">
	    <div class="col-lg-12">
				<div class="page-header">
					<h1>Teams</h1>
				</div>

				<div id="flash"></div>

				<div class="teams">
					<%= render :partial => 'teams', :object => @teams %>
				</div>

				<br><br>

				<div class="team_invitations">
				  <%= render :partial => 'team_invitations', :object => @team_invitations %>
				</div>
	    </div>
	  </div>
	</div>  

<% end %>