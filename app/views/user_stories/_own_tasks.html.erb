<div class="box">
	<div class="box-header">
		<div class="box-title pull-left">
      <h4>Your tasks</h4>
    </div>
  </div>
  <div class="box-content">
		<% if @own_tasks.empty? %>
			<h4>No Tasks taken yet..</h4><br>
		<% else %>
			<% @own_tasks.each do |task| %>
				<%= render :partial => 'user_stories/own_task', locals: {task: task} %>
			<% end %>
		<% end %>
  </div>
	<!--end: BOX CONTENT-->
</div>
<!--end: BOX-->