<%= render :partial => 'sidebar', :object => @project %>

<% if (current_user.has_role? :scrum_master, @project) || (current_user.has_role? :product_owner, @project) %>
	<div class="container-fluid">
    <div class="row">
      <div class="col-lg-12">
        <a href="#menu-toggle" class="btn btn-default" id="menu-toggle">Toggle Scrum Menu</a>

				<div class="page-header">
					<h1>Editing Project</h1>
				</div>

				<%= render 'form', project: @project %>

				<br>
      </div>
    </div>
	</div>  
	
<% end %>

<%= link_to 'Back', projects_path, class: "btn btn-primary" %>